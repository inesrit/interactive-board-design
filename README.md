
  # Interactive Board Design

  This is a code bundle for an Interactive Board Design. 

 ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  